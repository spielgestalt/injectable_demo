name: injectable_demo
description: Example usage of the package Injectable

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  injectable: ^0.2.2
  get_it: ^3.1.0
  retrofit: ^1.2.0
  json_annotation: ^3.0.1
  freezed_annotation:
  dio: ^3.0.8
dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.1.1
  build_runner:
  injectable_generator: ^0.2.2
  retrofit_generator: ^1.2.2+1
  json_serializable: ^3.2.5
  freezed:
flutter:

  uses-material-design: true

